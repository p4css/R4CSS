---
title: "quiz"
author: "Jilung Hsieh"
date: "2019/4/25"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# Library
```{r}
library(quanteda)
```

```{r}
df2 <- data.frame(x1 = c(4, 5, 6), x2 = c(2, 3, 4))
df1 <- dplyr::bind_rows(df1, df1)
```

# for-loop
```{r}
vec <- c(10, 20, 30, 40, 50)
for(i in 1:length(vec)){
    print(sprintf("%dth item: %d", i, vec[i]))
}
```

